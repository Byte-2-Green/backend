CREATE TABLE Food_for_Thought (
    FfT_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Title VARCHAR2(255) NOT NULL,
    Description CLOB NOT NULL,
    Category VARCHAR2(100) NOT NULL,
    Icon CLOB NOT NULL
);